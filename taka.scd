s.waitForBoot{a = KonaPlayer.new};

a.jatis[0].octave
"SrGmPdN+SrGmPdN S".count({ |item, i| item == $S})

"S".count({ |item, i| item == $S})

Jati(1,4,1)


("xx" ! 1)[0]=$X).reduce('++');

(("x" ! 1)[0]=$X).reduce('++')
x = "x".reduce('++') ?? "X"

SrGmPdN+S